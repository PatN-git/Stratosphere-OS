Your ONE and ONLY task is to run the scaffold script then print a sentinel:

Step 1 — Run this exact command from the project root:
  python .claude/plugins/stratosphere-os/scripts/scaffold.py

Step 2 — After the script exits, print exactly this string on its own line:
  HARNESS_DONE

That is all. No other actions. Do not explain. Do not ask questions. Just run the script and print HARNESS_DONE.
